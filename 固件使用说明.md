# 固件使用说明

## AP_StepMotor 步进电机闭环驱动

### 1. 概述

AP_StepMotor 是 ArduPilot 固件中用于驱动 **Emm_V5.0 / Emm42_V5.0 / ZDT_X42** 系列步进电机闭环伺服驱动板的驱动库。飞控通过 UART 串口与该驱动板通信，按板载自定义协议发送速度/位置指令，驱动步进电机旋转。

- 通讯方式：UART 串口，波特率由 `SERIALx_BAUD` 参数决定（如 57 = 57600，115 = 115200），需与驱动板 OLED 菜单 `P_Serial` 中设置的波特率一致
- 协议：Emm_V5 自定义协议（详见 `ref_docs/Emm_V5.0步进闭环驱动说明书Rev1.3.pdf`）
- 驱动库位置：`libraries/AP_StepMotor/`

### 2. 硬件连接与配置流程

#### 2.1 接线

| 飞控串口 | 驱动板 |
| --- | --- |
| TX | RX |
| RX | TX |
| GND | GND |

> 注意：驱动板侧需在 OLED 菜单中将 `P_Serial` 设置为对应串口通讯模式，波特率选择 115200。

#### 2.2 飞控配置步骤

1. **设置串口协议**：将空闲串口（如 `SERIAL2`）的 `SERIALx_PROTOCOL` 设为 **49**（StepMotor），`SERIALx_BAUD` 设为与驱动板一致的波特率（如 115 = 115200，或 57 = 57600）。
2. **设置舵机通道功能**：选择一个输出通道（如 `SERVO8_FUNCTION`），赋予其一个舵机功能（如 `78` 脚本输出或任意自定义功能），该通道的归一化输出（-1 ~ 1）将作为电机目标角度的来源。
3. **启用驱动**：设置 `SRV_STM_CHAN` 为上述通道号（1~16）。
4. **检查电机地址与细分**：`SRV_STM_ADDR` 需与驱动板 OLED 菜单中的 ID 一致（默认 1）；`SRV_STM_DIV` 需与驱动板 `P_Pul` 细分一致（默认 16）。
5. **上电测试**：飞控上电后驱动会发送使能命令，可在 Mission Planner 中观察 `STATUSTEXT` 提示。

### 3. 参数介绍

所有 AP_StepMotor 参数均以 `SRV_STM_` 为前缀，由 `SRV_Channels` 的参数组挂载。

#### 3.1 SRV_STM_CHAN — 控制通道

| 项目 | 内容 |
| --- | --- |
| 默认值 | 0 |
| 范围 | 0 ~ 16 |
| 说明 | 被跟随的舵机输出通道号。该通道的归一化输出（-1 ~ 1）乘以角度系数后作为电机目标角度。**0 表示禁用驱动**。 |

#### 3.2 SRV_STM_DT — 指令发送周期

| 项目 | 内容 |
| --- | --- |
| 默认值 | 100 |
| 单位 | ms |
| 范围 | 20 ~ 1000 |
| 说明 | 飞控以该周期采样舵机通道输出并向驱动板发送绝对位置指令（`0xFD`，raF=true）。目标角度变化不足 1 个脉冲时不重复发送，避免以固定速率重发相同目标刷爆总线。 |

#### 3.3 SRV_STM_SC — 角度比例系数

| 项目 | 内容 |
| --- | --- |
| 默认值 | 1.0 |
| 单位 | deg（满偏对应角度） |
| 范围 | 0.01 ~ 50 |
| 说明 | 通道满偏（归一化输出 = ±1）对应的目标角度，即 `目标角度 = 归一化输出 × SC × 360°`。例如：SC=0.5 时满杆对应 ±180°；SC=1 时满杆对应 ±360°。 |

#### 3.4 SRV_STM_VEL — 运动速度

| 项目 | 内容 |
| --- | --- |
| 默认值 | 500 |
| 单位 | RPM |
| 范围 | 0 ~ 5000 |
| 说明 | 发送位置指令时使用的电机转速。速度越高到达目标越快，但需注意驱动板最高转速限制。 |

#### 3.5 SRV_STM_ACC — 加速度档位

| 项目 | 内容 |
| --- | --- |
| 默认值 | 0 |
| 范围 | 0 ~ 255 |
| 说明 | 曲线加减速档位。**0 表示不使用加减速，直接以设定速度启动**。非 0 时加减速步进时间 = (256-ACC) × 50μs 每 RPM，数值越小加速越快。 |

#### 3.6 SRV_STM_DIV — 细分设置

| 项目 | 内容 |
| --- | --- |
| 默认值 | 16 |
| 范围 | 1 ~ 256 |
| 说明 | 驱动板 `P_Pul` 菜单中配置的细分值。对 1.8° 步进电机，每圈脉冲数 = 200 × 细分（如 16 细分 = 3200 脉冲/圈）。**必须与驱动板实际细分一致**，否则角度误差成倍。 |

#### 3.7 SRV_STM_ADDR — 电机地址

| 项目 | 内容 |
| --- | --- |
| 默认值 | 1 |
| 范围 | 1 ~ 255 |
| 说明 | 驱动板总线地址，须与驱动板 OLED 菜单中的 ID 一致。支持多机通讯时不同电机使用不同地址。 |

#### 3.8 SRV_STM_CKSUM — 校验方式

| 项目 | 内容 |
| --- | --- |
| 默认值 | 0 |
| 可选值 | 0:0x6B（板子默认），1:XOR，2:CRC8 |
| 说明 | 通讯校验方式，必须与驱动板 `Checksum` 菜单一致，否则驱动板不识别指令。三种校验算法见 `ref_docs/Emm_V5.0通讯校验算法.txt`。 |

#### 3.9 SRV_STM_FSACT — 失控保护动作

| 项目 | 内容 |
| --- | --- |
| 默认值 | 1 |
| 可选值 | 0:无，1:急停电机 |
| 说明 | RC 接收机信号失控（failsafe）时，向驱动板发送立即停止命令（`0xFE 0x98`）。**硬件安全开关处于安全位时，无论该参数如何设置，电机都会被急停**。若仅通过 MAVLink 控制、无 RC 接收机，请设为 0，否则会一直触发急停。 |

### 4. 参数速查表

| 参数 | 默认值 | 说明 |
| --- | --- | --- |
| SRV_STM_CHAN | 0 | 控制通道（0=禁用） |
| SRV_STM_DT | 100 | 指令发送周期 (ms) |
| SRV_STM_SC | 1.0 | 角度比例（满偏度数/360） |
| SRV_STM_VEL | 500 | 速度 (RPM) |
| SRV_STM_ACC | 0 | 加速度档位 (0=直接启动) |
| SRV_STM_DIV | 16 | 细分（须与驱动板 P_Pul 一致） |
| SRV_STM_ADDR | 1 | 电机地址（须与驱动板 ID 一致） |
| SRV_STM_CKSUM | 0 | 校验方式（0:0x6B / 1:XOR / 2:CRC8） |
| SRV_STM_FSACT | 1 | 失控动作（0=无 / 1=急停） |

### 5. 工作方式说明

- **控制链路**：`SRV_STM_CHAN` 指定通道的 PWM（1000~2000，1500 为零位）→ 目标角度 → 绝对位置指令（`0xFD`，脉冲数 = 目标角度 × 细分/1.8°）→ 驱动板执行；目标变化不足 1 脉冲时不重复发送。
- **状态轮询**：每 250ms（4 Hz）读取一次系统状态参数（`0x43` 命令），单帧返回实时位置、实时转速、位置误差、目标位置、母线电压、相电流、编码器值及就绪/电机状态标志位，供状态监控与日志使用。
- **反馈超时**：首次上电后 1s 内无任何反馈时按电机位于 0° 处理；连续 3s 无反馈则驱动板视为失联（`healthy()` 返回 false）。
- **安全机制**：安全开关合闸（SAFETY_DISARMED）或 RC 失控时立即急停；恢复后自动以当前反馈位置为基准重新同步，避免跳变。
- **支持的命令**：使能（`0xF3 0xAB`）、位置控制（`0xFD`）、立即停止（`0xFE 0x98`）、读取系统状态（`0x43`）；解析器同时兼容读取实时位置（`0x36`）、实时转速（`0x35`）、位置误差（`0x37`）、目标位置（`0x33`/`0x34`）等应答帧。

### 6. 遥测与日志（STPM 记录）

驱动每 200ms 将驱动板反馈的遥测数据写入飞控 DataFlash 日志（消息类型 **STPM**），用于拉力台数据分析：

| 字段 | 说明 |
| --- | --- |
| TimeUS | 时间戳 (μs) |
| CPos | 电机实时位置（°） |
| TPos | 电机目标位置（°） |
| CurTPos | 电机实时设定的目标位置（正在执行的）（°） |
| Vel | 电机实时转速 (RPM) |
| PErr | 电机位置误差（°） |

> 说明：
> - 位置类数据由驱动板返回的 0~65535 计数值（一圈）换算为角度：`角度 = 计数值 × 360 / 65536`，符号字节 0=正、1=负。
> - 转速单位为 RPM（转/分钟），符号字节同上。
> - 首次收到位置反馈前，日志不写入（`_feedback_valid` 为 false）。

### 7. 常见问题

**Q1：电机不动，GCS 提示 "command rejected (not enabled/stall)"**
- 检查电机是否已使能（En 引脚状态或驱动板菜单），以及是否触发堵转保护。
- 检查 `SRV_STM_ADDR` 是否与驱动板 ID 一致。

**Q2：电机旋转角度与实际不符（偏大/偏小）**
- 检查 `SRV_STM_DIV` 是否与驱动板 `P_Pul` 实际细分一致。

**Q3：指令全部无效**
- 检查 `SRV_STM_CKSUM` 是否与驱动板 `Checksum` 菜单一致。

**Q4：只通过地面站控制，不接遥控器，但电机一直急停**
- 将 `SRV_STM_FSACT` 设为 0。
